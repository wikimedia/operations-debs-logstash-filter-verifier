The source is 'repackaged' by including the 'vendor' directory generated by
'go mod vendor' in the upstream version. The result is then committed and
tagged as upstream/<version>+ds, to avoid fetching dependencies at build time.
